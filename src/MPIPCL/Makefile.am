noinst_LIBRARIES = libmpipcl.a

# TODO: Is there a better way to add these flags
# -lm is for math.h
AM_CFLAGS = -Wall -Wextra -Wpedantic -fopenmp -lm -lstdc++

libmpipcl_a_SOURCES = mpipcl.c send.c setup.c sync.c
